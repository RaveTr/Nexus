/**
 * Super-package containing different template packages for different types of objects, mostly with {@code PropertyWrapper}
 * implementations. Additionally, contains abstractions for different object types to streamline working with common
 * yet annoying use cases (e.g. creating a new type of grass block).
 */
package com.mememan.nexus.template;