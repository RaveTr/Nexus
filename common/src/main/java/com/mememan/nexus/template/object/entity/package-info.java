/**
 * Contains item helpers/extensions that abstract common yet annoying custom implementations, as well as some needed
 * implementations that complement existing template objects of other types (e.g. for boats).
 */
package com.mememan.nexus.template.object.entity;