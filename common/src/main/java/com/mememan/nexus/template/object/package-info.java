/**
 * Contains helper object classes that abstract annoyances/oddities you'd otherwise have to deal with for some common
 * implementations, depending on the sub-package's object type ({@code "block"} would contain a custom block
 * extension to abstract the addition of custom grass/dirt/farmland, etc.).
 */
package com.mememan.nexus.template.object;