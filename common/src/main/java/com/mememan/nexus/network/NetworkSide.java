package com.mememan.nexus.network;

/**
 * An object-holder {@code enum} representing network sides.
 */
public enum NetworkSide {
    /**
     * C2S (Client to Server).
     */
    C2S,
    /**
     * S2C (Server to Client).
     */
    S2C
}
